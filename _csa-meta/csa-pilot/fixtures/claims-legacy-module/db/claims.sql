-- Claims schema (Oracle-style)
CREATE TABLE CLAIM (
  CLAIM_ID   NUMBER PRIMARY KEY,
  POLICY_ID  VARCHAR2(32) NOT NULL,
  STATUS     VARCHAR2(32) NOT NULL
);

CREATE INDEX IX_CLAIM_POLICY ON CLAIM (POLICY_ID);
